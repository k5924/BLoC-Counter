name: bloc_counter
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
    sdk: ">=2.12.0 <3.0.0"

dependencies:
    bloc: ^7.1.0
    bloc_test: ^8.1.0
    cupertino_icons: ^1.0.2
    flutter:
        sdk: flutter
    flutter_bloc: ^7.2.0

dev_dependencies:
    flutter_lints: ^1.0.0
    flutter_test:
        sdk: flutter
    lint: ^1.7.2
    mocktail: ^0.1.4

flutter:
    uses-material-design: true
